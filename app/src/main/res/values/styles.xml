<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="CongratulationTextAppearance" parent="TextAppearance.AppCompat">

        <item name="android:textSize">@dimen/congratulationTextHeight</item>
        <item name="android:textColor">@color/congratulationTextColor</item>
        <item name="android:textStyle">bold|italic</item>
        <item name="android:singleLine">true</item>
    </style>

    <style name="FromTextAppearance" parent="TextAppearance.AppCompat">

        <item name="android:textSize">@dimen/congratulationTextHeight</item>
        <item name="android:textColor">@color/fromTextColor</item>
        <item name="android:textStyle">bold|italic</item>
        <item name="android:singleLine">true</item>
    </style>

</resources>
